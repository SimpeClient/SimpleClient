plugins {
}

version = project.simpleclient_version
group = 'simpleclient'

repositories {
}

dependencies {
}

subprojects {
}

allprojects {
    version = rootProject.version
    group = rootProject.group
}